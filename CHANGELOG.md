# Changelog

All notable changes to `rc_shield` should be documented in this file.

## [Unreleased]

- Ongoing development builds use `rc_shield::PLUGIN_VERSION` with a `+dev` suffix until the next release is cut.

## [1.0.0] - 2026-04-11

- Formalized the plugin's self-metadata through `rc_shield::PLUGIN_VERSION` and `rc_shield::info()`.
- Aligned self-versioning with a cleaner release workflow while keeping existing plugin behavior intact.
